{
  "name": "fast-send-platform",
  "version": "1.0.0",
  "description": "Fast Send Hybrid Mobile App + Admin Web Portal + Realtime Backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd hybrid-app && npm install && npm run build && cd ../admin-portal && npm install && npm run build && cd ../backend && npm install",
    "start:backend": "cd backend && node server.js",
    "start:app": "cd hybrid-app && npm run dev",
    "start:admin": "cd admin-portal && npm run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "pngjs": "^7.0.0"
  }
}
